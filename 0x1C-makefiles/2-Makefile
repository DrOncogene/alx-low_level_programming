# _*_Makefile_*_

CC = gcc
SRC = main.c school.c
OBJ = $(SRC:%.c=%.o)
NAME = school

all: ${OBJ} school

school: ${OBJ}
	gcc ${OBJ} -o ${NAME}

%: %.c m.h
	${CC} -c $< -o $@

clean:
	rm -f *.o school
